### Start
    php -S localhost:8000 -t ./public
    
### Build Javascript
    gulp            # watch files for changes and compile
    gulp compile    # compile once
    
### Build Project
    npm install